#!/bin/sh

/usr/bin/mysql --password=`cat /karchan2/mud/sql/password.sql` mud <<END_OF_DATA

delete from tmp_itemtable where id=$1;
delete from itemtable where id=$1;
delete from items where id=$1;

END_OF_DATA

